using GLJ.Controllers;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;
using Microsoft.VisualStudio.TestTools.UnitTesting.Web;
using System.Web.Mvc;

namespace GLJWebTest
{
    
    
    /// <summary>
    ///This is a test class for PedidoControllerTest and is intended
    ///to contain all PedidoControllerTest Unit Tests
    ///</summary>
    [TestClass()]
    public class PedidoControllerTest
    {
        [TestMethod()]
        public void IndexTest()
        {
            PedidoController target = new PedidoController(); // TODO: Initialize to an appropriate value
            Nullable<int> codigoPedido = new Nullable<int>(); // TODO: Initialize to an appropriate value
            ActionResult expected = null;
            ActionResult actual;

            //actual = target.Index(CodigoPedido);
            //Assert.AreEqual(expected, actual);

            //CodigoPedido = 3;
            //actual = target.Index(CodigoPedido);
            //Assert.AreEqual(expected, actual);
        }
    }
}
